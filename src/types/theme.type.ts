export interface ITheme {
  // font theme
  tiny?: boolean;
  small?: boolean;
  medium?: boolean;
  large?: boolean;
  huge?: boolean;

  // color theme
  standard?: boolean;
  warm?: boolean;
  dim?: boolean;
  dark?: boolean;
}
